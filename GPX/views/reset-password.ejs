<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/support.css' />
</head>

<body class="support-page">
    <%- include('partials/header') %>

        <div class="contact-form-container">
            <div class="container">
                <h1 class="contact-title">Cambiar contraseña de inicio de sesión</h1>
                <p class="contact-description">Si has olvidado tu contraseña o deseas cambiarla, introduce tu correo
                    electrónico y te ayudaremos a restablecerla.</p>

                <% if (typeof error !=='undefined' && error) { %>
                    <div class="error-message"
                        style="background: #ffebee; color: #c62828; padding: 12px; border-radius: 4px; margin-bottom: 20px; border-left: 4px solid #c62828;">
                        <%= error %>
                    </div>
                    <% } %>

                        <% if (typeof success !=='undefined' && success) { %>
                            <div class="success-message"
                                style="background: #e8f5e9; color: #2e7d32; padding: 12px; border-radius: 4px; margin-bottom: 20px; border-left: 4px solid #2e7d32;">
                                <%= success %>
                            </div>
                            <% } %>

                                <form class="contact-form" action="/support/reset-password" method="POST">
                                    <div class="form-group">
                                        <label for="email" class="form-label">Correo electrónico:</label>
                                        <input type="email" id="email" name="email" class="form-input"
                                            placeholder="correo@example.com" required>
                                    </div>

                                    <div class="form-group">
                                        <label for="newPassword" class="form-label">Nueva contraseña:</label>
                                        <input type="password" id="newPassword" name="newPassword" class="form-input"
                                            placeholder="Introduce tu nueva contraseña" required minlength="6">
                                    </div>

                                    <div class="form-group">
                                        <label for="confirmPassword" class="form-label">Confirmar nueva
                                            contraseña:</label>
                                        <input type="password" id="confirmPassword" name="confirmPassword"
                                            class="form-input" placeholder="Confirma tu nueva contraseña" required
                                            minlength="6">
                                    </div>

                                    <button type="submit" class="btn btn-submit">Cambiar contraseña</button>
                                </form>
            </div>
        </div>

        <%- include('partials/footer') %>

</body>

</html>